<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nikita.popularmovies.movies_repository"
    >

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        >
        <provider
            android:name="com.nikita.popularmovies.common.provider.MoviesContentProvider"
            android:authorities="com.nikita.popularmovies"
            android:exported="true"/>
    </application>

</manifest>
